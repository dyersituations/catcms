<div class="banner">
  <%= image_tag @page.banner_url(:banner).to_s %>
</div>

<div class="content">
    <% if @view %>
        <%= render @view %>
    <% else %>
        <div class="plain">
            <%= simple_format(@page.content).html_safe %>
        </div>
    <% end %>
</div>