<h1>Editing page</h1>

<%= render 'form' %>

<%= link_to 'Cancel', page_view_path(@page.path) %>
<%= button_to 'Remove Page', @page, :confirm => 'Are you sure you want to remove this page permanently?', :method => :delete %>
