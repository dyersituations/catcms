<div id="page_top" class="bar">
  <%= link_to root_url do %>
      <div id="page_top_title">
        <div class="main">Ann Fleming</div>
        <div class="sub">Sculptural Art</div>
      </div>
  <% end %>

  <div class="tabs">
    <% @pages.each do |page| %>
      <div><%= link_to page.title, root_url + page.path %></div>
    <% end %>
  </div>
</div>
