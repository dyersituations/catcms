<div id="gallery-wrapper">
  <% if @posts.each do |post| %>
    <a href="<%= image_path(post.image.full) %>" rel="lightbox[gallery]"
        data-title="<%= post.title %>"
        data-caption="<%= simple_format(h(post.content)) %>">
      <%= image_tag post.image.thumb %>
    </a>
  <% end.empty? %>
    No images.
  <% end %>
  <div class="clear"></div>
</div>